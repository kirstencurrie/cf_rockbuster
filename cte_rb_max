/* Here is SQL query to calculate the max, average, and count of the 'rental_duration' value */

SELECT MIN(rental_duration) AS min_rent_duration,
 MAX(rental_duration) AS max_rent_duration,
 AVG(rental_duration) AS avg_rent_duration,
 COUNT(rental_duration) AS count_rent_duration_values,
 COUNT(*) AS count_rows
FROM film;

Here is SQL query to calculate the max, average, and count of the 'rental_rate' value

SELECT MIN(rental_rate) AS min_rental_rate,
 MAX(rental_rate) AS min_rental_rate,
 AVG(rental_rate) AS min_rental_rate,
 COUNT(rental_rate) AS count_rental_rates,
 COUNT(*) AS count_rows
FROM film;
